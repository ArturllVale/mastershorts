export async function renderInBrowser(options) {
  // A simple stub returning a blank video blob URL or we can try to call a real endpoint if needed.
  // We'll throw an error if it's actually used and fails.
  console.warn("renderInBrowser is a stub", options);
  return "";
}
