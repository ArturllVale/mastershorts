{
  "name": "master-shorts",
  "version": "1.0.0",
  "description": "AI-powered vertical video shorts generator platform",
  "scripts": {
    "dev:backend": "node scripts/dev-backend.js",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:renderer": "npm --prefix render-service run dev",
    "dev": "concurrently -n \"backend,frontend,renderer\" -c \"blue,green,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:renderer\"",
    "build:frontend": "npm --prefix frontend run build",
    "install:frontend": "npm --prefix frontend install",
    "install:renderer": "npm --prefix render-service install",
    "install:remotion": "npm --prefix remotion install",
    "install:backend": "node scripts/install-backend.js",
    "install:all": "npm install && npm run install:frontend && npm run install:renderer && npm run install:remotion && npm run install:backend",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^8.2.2",
    "eslint": "^10.11.0",
    "globals": "^17.12.0"
  }
}
